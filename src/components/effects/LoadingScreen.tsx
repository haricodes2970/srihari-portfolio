// ============================================================
// LOADING SCREEN — srihari-portfolio
// TODO: Three swords animate in, blade fills, fades out
// Uses: useUIStore (isLoading, setLoading)
// Must hide after window load + min 2s
// ============================================================

"use client";

export default function LoadingScreen() {
  return (
    <div id="loading-screen">
      {/* TODO: Three sword SVGs + energy bar + text */}
    </div>
  );
}
