// ============================================================
// PARTICLE CANVAS — srihari-portfolio
// TODO: Canvas-based sword trail following cursor
// Uses: HTML Canvas API + requestAnimationFrame
// Only active in Zoro mode
// Keep performant: draw green glow particles, fade over time
// ============================================================

"use client";

export default function ParticleCanvas() {
  return (
    <canvas id="particle-canvas">
      {/* TODO: Canvas sword trail */}
    </canvas>
  );
}
